# 🧾 SabAI Bill MVP

**SabAI Bill** — это интеллектуальный инструмент для парсинга и разделения чеков, работающий полностью **offline**. Приложение использует OCR для распознавания текста, переводит позиции с помощью современных нейросетевых моделей (NLLB-200) и автоматически распределяет затраты между участниками.

---

## 🚀 Основные возможности

* **Мультиязычный OCR**: Распознавание текста с помощью *PaddleOCR*. Поддерживаются кириллица, латиница, тайский, китайский, японский, корейский и арабский языки.
* **Offline Перевод**: Интеграция с моделью **NLLB-200** от Meta, обеспечивающей высокое качество перевода для редких языковых пар.
* **Умный парсинг**: Алгоритмы автоматического извлечения структуры чека: наименование товара, количество, цена и итоговая сумма по позиции.
* **Категоризация**: Автоматическое определение категорий (Food, Drinks, Service и др.) на основе правил для английского языка.
* **Split Engine**: Гибкая логика разделения общего счета между группами людей (A, B, C, D) с защитой от ошибок ввода.

---

## 📂 Структура проекта

```text
SabAI_Bill_MVP/
│
├── app.py              # Интерфейс Streamlit и дирижирование логикой
├── ocr_module.py       # Обработка изображений и OCR (PaddleOCR)
├── translator.py       # Модуль офлайн-перевода (NLLB-200)
├── parser.py           # Извлечение структуры данных из сырого текста
├── category_module.py  # Правила классификации товаров
├── split_engine.py     # Логика расчета долей в счете
└── requirements.txt    # Список необходимых зависимостей

## 🛠 Технологический стек

| Библиотека | Версия | Описание | 
| Python | 3.10.11 | Основной язык разработки |
| Streamlit | latest | Веб-интерфейс приложения |
| PaddleOCR | 2.7.0.3 | Движок распознавания текста |
| NLLB-200 | via HF | Offline перевод (AutoModelForSeq2SeqLM) |
| OpenCV | 4.6.0.66 | Препроцессинг изображений |
| NumPy | 1.26.4 | Математические вычисления (совместимая версия) |
| PyTorch | 2.0.1+cpu | Среда для запуска нейросетевых моделей |

## ⚙️ Установка и запуск

1. Подготовка окружения
Рекомендуется использовать виртуальное окружение:
python -m venv venv
# Для Windows:
venv\Scripts\activate

2. Установка зависимостей
pip install -r requirements.txt

3. Запуск приложения
python -m streamlit run app.py

## ⚠️ Важные примечания

[!IMPORTANT] Первый запуск: Приложение скачает веса моделей (около 3-4 ГБ). 
Потребуется свободное место на диске и интернет.

Совместимость: Обязательно используйте numpy==1.26.4, чтобы избежать конфликтов с PaddleOCR.

## 💡 Как это работает

1. Загрузка: Загрузка фото чека в интерфейс.

2. Распознавание: Извлечение текста с учетом региональных особенностей.

3. Перевод: Перевод позиций через NLLB-200.

4. Разделение: Выбор участников для каждой позиции и расчет итогов.
